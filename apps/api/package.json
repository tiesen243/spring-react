{
  "name": "@yuki/api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "./gradlew build",
    "clean": "git clean -xdf .cache .gradle .settings .turbo bin build .classpath .project",
    "dev": "./gradlew bootRun --continuous",
    "start": "./gradlew bootRun"
  }
}
